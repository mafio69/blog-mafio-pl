{% extends 'base.html.twig' %}

{% block title %}RSS Feeds — Admin{% endblock %}

{% block body %}
    <h1>RSS Feeds</h1>
    <p>
        <a href="{{ path('admin_dashboard') }}">← Dashboard</a> |
        <a href="{{ path('logout') }}">Logout</a>
    </p>

    {# Feed list temporarily disabled due to Supabase schema issues #}
    <p style="color: #666; padding: 2rem; background: #f9f9f9; border: 1px solid #ddd;">
        Feed management is temporarily disabled.
    </p>
{% endblock %}
