{% extends 'base.html.twig' %}

{% block title %}Admin — blog.mafio.pl{% endblock %}

{% block body %}
    <h1>Admin Dashboard</h1>
    <p>Zalogowany jako: {{ app.user.userIdentifier }}</p>
    <nav>
        <a href="{{ path('admin_project_state') }}">📋 Project State</a> |
        <a href="{{ path('logout') }}">Wyloguj</a>
    </nav>
{% endblock %}
